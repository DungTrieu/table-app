import { Button } from "@material-ui/core";

const DelButton = () => {
    return (
        <div>
            <Button variant='contained'>Delete</Button>
        </div>
    )
}

export default DelButton